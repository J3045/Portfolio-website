"use client";
import SectionHeading from "./section-heading";
import { experiencesData } from "@/lib/data";
import { useSectionInView } from "@/lib/hook";
import { motion } from "framer-motion";
export default function Experience(){const {ref}=useSectionInView("Experience");return <section id="experience" ref={ref} className="mb-32 w-full max-w-6xl scroll-mt-28"><SectionHeading>Experience</SectionHeading><div className="grid gap-4">{experiencesData.map((item,index)=><motion.article key={index} initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true}} whileHover={{scale:1.01}} className="surface grid gap-5 rounded-3xl p-6 sm:grid-cols-[160px_1fr] sm:p-7"><div><p className="text-xs font-semibold text-violet-600 dark:text-violet-300">{item.date}</p><p className="mt-2 text-sm text-slate-500 dark:text-white/40">{item.location}</p></div><div><h3 className="text-xl font-semibold text-slate-950 dark:text-white">{item.title}</h3><p className="mt-1 font-medium text-slate-600 dark:text-white/55">{item.companyname}</p><p className="mt-4 max-w-3xl leading-7 text-slate-600 dark:text-white/50">{item.description}</p></div></motion.article>)}</div></section>;}